<div class="row">
  <%= link_to "添加", new_photo_category_path, class: "btn btn-info" %>
  <hr>
  <table class="table table-striped table-bordered table-hover table-condensed">
    <thead>
      <tr class="active">
        <th>Id</th>
        <th>分类名</th>
        <th>创建时间</th>
        <th>操作</th>
      </tr>
    </thead>
    <tbody>
      <% @photo_categories.each do |photo_category|%>
        <tr>
          <td><%= photo_category.id%></td>
          <td><%= photo_category.name%></td>
          <td><%= photo_category.created_at.strftime('%Y-%-m-%-d %H:%M:%S')%></td>
          <td><%= link_to "修改", edit_photo_category_path(photo_category), class: "btn btn-warning" %>
            <%= link_to "删除", "/photo_categories/delete?id=#{photo_category.id}" , class: "btn btn-danger" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>